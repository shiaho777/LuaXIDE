Bundled no-root proot userland (Termux builds).

Layout:
  <abi>/bin/proot
  <abi>/lib/libtalloc.so*
  <abi>/lib/libandroid-shmem.so
  <abi>/libexec/proot/loader

Installed at runtime to:
  filesDir/proot-rootfs/

No device root required.
